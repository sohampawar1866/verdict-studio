---
phase: 3
plan: 3
name: Live Streaming Debate Viewer & Canvas Node Glow
wave: 2
depends_on: [1, 2]
files_modified:
  - frontend/components/StreamingConsole.tsx
  - frontend/app/dag-studio/page.tsx
requirements: [R8, R9]
---

# Plan 03-03: Live Streaming Debate Viewer & Canvas Node Glow

## Objective
Build the split-pane live debate streaming console (`StreamingConsole.tsx`) and wire real-time WebSocket token streaming to animate active nodes with pulsing glows directly on the DAG Studio canvas.

## Tasks

<task type="auto">
  <name>Task 1: Build Live Streaming Debate Console</name>
  <files>frontend/components/StreamingConsole.tsx</files>
  <action>
    1. Create `frontend/components/StreamingConsole.tsx` with:
       - Real-time token streaming message stream grouped by debater.
       - Role color badges: Prosecutor (Crimson), Defense (Emerald), FactChecker (Sky), Chief Justice (Amber).
       - Live execution status bar with active token count, latency timer, and final verdict badge.
       - Collapse / Expand split-pane controls.
  </action>
  <verify>test -f frontend/components/StreamingConsole.tsx</verify>
  <done>StreamingConsole component created.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Execution, WebSocket Streaming & Canvas Glow into DAG Studio</name>
  <files>frontend/app/dag-studio/page.tsx</files>
  <action>
    1. In `frontend/app/dag-studio/page.tsx`, connect to `ws://localhost:8000/ws/telemetry`.
    2. Add "Run Debate Simulation" button to the top toolbar with spinning indicator during execution.
    3. On `NODE_ACTIVATED` event, update the corresponding node's `executionState="running"` to trigger visual glow on the canvas.
    4. On `TOKEN_CHUNK` event, stream tokens into `StreamingConsole` and node subtitle in real time.
    5. On `NODE_COMPLETED` event, update node `executionState="completed"` and attach final scores.
    6. Wire `CodeExportModal` into the "Export Python" button in toolbar.
  </action>
  <verify>test -f frontend/app/dag-studio/page.tsx</verify>
  <done>DAG Studio fully integrated with live streaming debate viewer and code exporter.</done>
</task>

## Must-Haves
- [x] Live debate viewer displays token-by-token text streams over WebSockets.
- [x] Canvas nodes visibly glow and pulse when active during debate simulation.
- [x] Export Python modal opens and provides 1-click standalone code.
