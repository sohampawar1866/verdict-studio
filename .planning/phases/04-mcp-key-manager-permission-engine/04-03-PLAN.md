---
phase: 4
plan: 3
name: Frontend Scoped Key Manager & Config Snippet Generator
wave: 2
depends_on: [1, 2]
files_modified:
  - frontend/components/KeyModal.tsx
  - frontend/components/ConfigSnippetModal.tsx
  - frontend/app/mcp-keys/page.tsx
requirements: [R11, R12, R14, R15]
---

# Plan 04-03: Frontend Scoped Key Manager & Config Snippet Generator

## Objective
Build `KeyModal.tsx` for key creation with granular permissions, `ConfigSnippetModal.tsx` for 1-click Claude Desktop configuration generation, and `frontend/app/mcp-keys/page.tsx` for key management, active status monitoring, and key revocation.

## Tasks

<task type="auto">
  <name>Task 1: Build Key Creation & Config Snippet Modals</name>
  <files>
    frontend/components/KeyModal.tsx
    frontend/components/ConfigSnippetModal.tsx
  </files>
  <action>
    1. Create `frontend/components/KeyModal.tsx`:
       - Key name input (e.g. `Claude Desktop Production`).
       - Granular tool permission toggles:
         - `db_query` checkbox + Strict Read-Only SQL toggle (blocks DROP/DELETE/ALTER).
         - `bash` terminal checkbox (with red danger badge).
         - `fetch_web` checkbox + domain whitelist editor.
         - Inline Verdict Debate on tool returns toggle.
       - Upon creation: shows raw key in highlighted green box with 1-click copy + copyable `claude_desktop_config.json` snippet.
    2. Create `frontend/components/ConfigSnippetModal.tsx`:
       - 1-Click JSON config viewer for Claude Desktop, Cursor IDE, and Devin agent integration.
  </action>
  <verify>test -f frontend/components/KeyModal.tsx && test -f frontend/components/ConfigSnippetModal.tsx</verify>
  <done>KeyModal and ConfigSnippetModal created and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Build Scoped MCP Keys Management Page</name>
  <files>frontend/app/mcp-keys/page.tsx</files>
  <action>
    1. Create `frontend/app/mcp-keys/page.tsx`:
       - Header with "Generate Scoped MCP Key" button.
       - Keys table displaying: Key Name, Display Prefix (`haize_mcp_live_...`), Scoped Permissions (tags for Read-Only SQL, Bash Blocked, Verdict Guard, Allowed Domains), Created Date, Status Badge (`ACTIVE`).
       - Actions: "View Config Snippet", "Revoke Key".
       - Empty state prompt when no keys exist.
  </action>
  <verify>test -f frontend/app/mcp-keys/page.tsx</verify>
  <done>MCP Keys page built and wired to backend REST API.</done>
</task>

## Must-Haves
- [x] Key creation modal supports granular permissions (SQL read-only, bash quarantine, Verdict debate).
- [x] Table displays all active scoped keys with revocation actions and 1-click config generator.
