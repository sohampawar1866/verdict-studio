---
phase: 5
plan: 1
name: TypeScript MCP Server with Tool Routing & Key Header Auth
wave: 1
depends_on: []
files_modified:
  - mcp-gateway/package.json
  - mcp-gateway/src/types.ts
  - mcp-gateway/src/index.ts
requirements: [R16, R17]
---

# Plan 05-01: TypeScript MCP Server with Tool Routing & Key Header Auth

## Objective
Implement the TypeScript MCP Server using `@modelcontextprotocol/sdk` and `StdioServerTransport`, supporting CLI args `--key` and `--backend-url`, exposing standard tool definitions (`db_query`, `fetch_web`, `bash`, `read_file`), and proxying calls to FastAPI backend with `X-Haize-MCP-Key`.

## Tasks

<task type="auto">
  <name>Task 1: Update Gateway Dependencies & Types</name>
  <files>
    mcp-gateway/package.json
    mcp-gateway/src/types.ts
  </files>
  <action>
    1. Ensure `mcp-gateway/package.json` has `build`, `start`, and executable bin entries.
    2. Add tool request schemas and response interfaces in `mcp-gateway/src/types.ts`.
  </action>
  <verify>test -f mcp-gateway/package.json && test -f mcp-gateway/src/types.ts</verify>
  <done>Gateway dependencies and interfaces configured.</done>
</task>

<task type="auto">
  <name>Task 2: Build Complete MCP Server with Stdio Transport</name>
  <files>mcp-gateway/src/index.ts</files>
  <action>
    1. In `mcp-gateway/src/index.ts`:
       - Parse `--key` and `--backend-url` CLI arguments or environment variables (`HAIZE_MCP_KEY`, `HAIZE_BACKEND_URL`).
       - Initialize `Server` from `@modelcontextprotocol/sdk/server/index.js`.
       - Implement `ListToolsRequestSchema` listing `db_query`, `fetch_web`, `bash`, `read_file`.
       - Implement `CallToolRequestSchema` routing tool executions to `POST /api/mcp/execute-tool`.
       - Ensure all logging strictly writes to `console.error`.
       - Connect `StdioServerTransport`.
  </action>
  <verify>npx tsx mcp-gateway/src/index.ts --help || true</verify>
  <done>MCP Server implemented with StdioServerTransport and tool handlers.</done>
</task>

## Must-Haves
- [x] Stdio transport handles JSON-RPC without contaminating stdout.
- [x] Supports `db_query`, `fetch_web`, `bash`, `read_file` with backend policy validation.
