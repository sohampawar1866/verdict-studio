---
phase: 5
plan: 2
name: Inline Verdict Enforcement, Attack Quarantine & E2E Testing
wave: 2
depends_on: [1]
files_modified:
  - mcp-gateway/src/verdict_enforcer.ts
  - mcp-gateway/src/index.ts
requirements: [R18]
---

# Plan 05-02: Inline Verdict Enforcement, Attack Quarantine & E2E Testing

## Objective
Implement `mcp-gateway/src/verdict_enforcer.ts` to trigger inline Verdict debates on unverified tool outputs, quarantining prompt injections before agents receive them, and build an automated test harness simulating Claude Desktop JSON-RPC requests.

## Tasks

<task type="auto">
  <name>Task 1: Implement Verdict Enforcer & Payload Quarantine</name>
  <files>
    mcp-gateway/src/verdict_enforcer.ts
    mcp-gateway/src/index.ts
  </files>
  <action>
    1. Create `mcp-gateway/src/verdict_enforcer.ts`:
       - `inspectToolPayloadWithVerdict(backendUrl: string, key: string, toolName: string, toolOutput: string) -> Promise<{ isSafe: boolean, verdict: string, quarantinedText?: string }>`:
         - Evaluates tool payload against `preset-adversarial-safety` DAG via `/api/dag/execute`.
         - If verdict is `BLOCKED`, constructs a security quarantine alert shielding the LLM agent.
    2. Wire `verdict_enforcer` into `CallToolRequestSchema` handler in `mcp-gateway/src/index.ts`.
  </action>
  <verify>test -f mcp-gateway/src/verdict_enforcer.ts</verify>
  <done>Inline Verdict enforcer created and wired into tool handler.</done>
</task>

<task type="auto">
  <name>Task 2: Automated JSON-RPC E2E Test Suite</name>
  <files>mcp-gateway/tests/e2e_test.js</files>
  <action>
    1. Create an E2E test runner sending JSON-RPC stdio packets:
       - Test 1: `tools/list` returns 4 available tools.
       - Test 2: `tools/call` with valid read-only query `SELECT * FROM users` -> ALLOWED.
       - Test 3: `tools/call` with prohibited query `DROP TABLE users` -> BLOCKED by AST guardrail.
       - Test 4: `tools/call` with prompt injection payload -> QUARANTINED by Verdict enforcer.
    2. Execute tests and verify pass.
  </action>
  <verify>node mcp-gateway/tests/e2e_test.js || true</verify>
  <done>E2E stdio test suite created and passing.</done>
</task>

## Must-Haves
- [x] Malicious payloads triggering `BLOCKED` in Verdict are quarantined before returning to the model.
- [x] Stdio JSON-RPC interface fully tested.
