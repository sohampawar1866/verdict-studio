---
phase: 7
plan: 1
name: Comprehensive End-to-End Integration Test Suite
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_integration.py
  - backend/tests/__init__.py
requirements: [R23, R24]
---

# Plan 07-01: Comprehensive End-to-End Integration Test Suite

## Objective
Build and run `backend/tests/test_integration.py` to verify the complete full-stack workflow: DAG lifecycle, pipeline compilation, token streaming, key creation, AST SQL guardrails, tool proxying, and audit telemetry.

## Tasks

<task type="auto">
  <name>Task 1: Build Integration Test Suite</name>
  <files>
    backend/tests/test_integration.py
    backend/tests/__init__.py
  </files>
  <action>
    1. Create `backend/tests/test_integration.py`:
       - Test 1: Health endpoint returns status ok and active counters.
       - Test 2: DAG CRUD (save, retrieve, delete).
       - Test 3: DAG Pipeline Execution with token streaming and leaf node resolution.
       - Test 4: MCP Key Creation and SHA-256 validation.
       - Test 5: AST SQL Guardrail: permits `SELECT` and blocks `DROP TABLE` / `DELETE FROM`.
       - Test 6: Prohibited Tool Check: blocks `bash` on scoped key.
       - Test 7: Audit Log inspection reflects accurate entries and latency.
       - Test 8: Python Code Exporter syntax validation via `ast.parse()`.
    2. Export in `backend/tests/__init__.py`.
  </action>
  <verify>python3 -m unittest backend/tests/test_integration.py</verify>
  <done>Integration test suite built and passing 100%.</done>
</task>

## Must-Haves
- [x] All 8 integration test scenarios pass with zero failures.
