---
phase: 3
plan: 1
name: Backend Verdict Pipeline Executor & Streaming Hub
wave: 1
depends_on: []
files_modified:
  - backend/app/engine/verdict_runner.py
  - backend/app/engine/live_streamer.py
  - backend/app/engine/__init__.py
  - backend/app/main.py
requirements: [R9, R10]
---

# Plan 03-01: Backend Verdict Pipeline Executor & Streaming Hub

## Objective
Build `backend/app/engine/verdict_runner.py` and `backend/app/engine/live_streamer.py` to compile DAG graphs into executable pipeline structures, run executions with live WebSocket token streaming, and expose `/api/dag/execute` and `/api/dag/export-code` endpoints.

## Tasks

<task type="auto">
  <name>Task 1: Build Verdict Runner & Live Streamer</name>
  <files>
    backend/app/engine/verdict_runner.py
    backend/app/engine/live_streamer.py
    backend/app/engine/__init__.py
  </files>
  <action>
    1. Create `backend/app/engine/verdict_runner.py`:
       - Function `compile_and_run_dag(dag: DAGGraph, inputs: dict, ws_broadcast=None) -> DAGExecutionResponse`.
       - Topological sorting of DAG nodes to resolve dependency layers.
       - Token-by-token streaming loop emitting `NODE_ACTIVATED`, `TOKEN_CHUNK`, and `NODE_COMPLETED` events over WebSocket.
       - Computes final ruling and output scores for Chief Justice / Judges / Aggregators.
    2. Create `backend/app/engine/live_streamer.py` with mock/real streaming generators and debate simulation dialogues.
    3. Export in `backend/app/engine/__init__.py`.
  </action>
  <verify>python3 -c "import sys; sys.path.insert(0, 'backend'); from app.engine import compile_and_run_dag; print('Verdict Runner OK')"</verify>
  <done>Verdict runner and streaming engine created and verified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Execution & Code Export REST Endpoints</name>
  <files>backend/app/main.py</files>
  <action>
    1. In `backend/app/main.py`, add `/api/dag/execute` POST endpoint accepting `DAGExecutionRequest`.
    2. Trigger background execution with real-time broadcasting over `ws_manager.broadcast()`.
    3. Add `/api/dag/export-code` POST endpoint returning generated standalone Python script string.
  </action>
  <verify>python3 -c "import sys; sys.path.insert(0, 'backend'); from app.main import app; print('Endpoints Ready')"</verify>
  <done>Execution and code export endpoints integrated into FastAPI.</done>
</task>

## Must-Haves
- [x] `compile_and_run_dag` parses arbitrary DAG topologies and streams tokens over WebSockets.
- [x] `/api/dag/execute` executes pipelines asynchronously while broadcasting live events.
