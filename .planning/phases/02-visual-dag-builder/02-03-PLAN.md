---
phase: 2
plan: 3
name: DAG Studio Canvas Page & Backend Persistence
wave: 2
depends_on: [1, 2]
files_modified:
  - frontend/components/Canvas.tsx
  - frontend/app/dag-studio/page.tsx
  - backend/app/main.py
requirements: [R3, R7]
---

# Plan 02-03: DAG Studio Canvas Page & Backend Persistence

## Objective
Assemble the complete DAG Studio workspace in `frontend/app/dag-studio/page.tsx` with `@xyflow/react` Canvas, toolbar controls, preset loader, node drawer, and backend REST endpoints for saving/retrieving DAGs.

## Tasks

<task type="auto">
  <name>Task 1: Build Core Canvas Component</name>
  <files>frontend/components/Canvas.tsx</files>
  <action>
    1. Create `frontend/components/Canvas.tsx` wrapping React Flow with:
       - `nodeTypes` mapping to our 7 custom nodes.
       - Custom Edge styling with animated gradients and delete action on hover.
       - Drag-and-drop drop-listener (`onDrop`, `onDragOver`).
       - Canvas controls: Background grid (dots), MiniMap, Zoom/Pan controls, Reset view.
       - Selection management (`onNodeClick`, `onPaneClick`).
  </action>
  <verify>test -f frontend/components/Canvas.tsx</verify>
  <done>Canvas component ready with interactive flow mechanics.</done>
</task>

<task type="auto">
  <name>Task 2: Build DAG Studio Page with Toolbars & Preset Loader</name>
  <files>frontend/app/dag-studio/page.tsx</files>
  <action>
    1. Create `frontend/app/dag-studio/page.tsx` integrating:
       - Top toolbar with DAG title, preset selector dropdown ("Adversarial Safety", "G-Eval Coherence", "Ensemble Verify"), "Clear Canvas", "Save DAG", "Export JSON", "Import JSON".
       - Left sidebar with collapsible `NodePalette`.
       - Central workspace with `Canvas`.
       - Right drawer with `NodeConfigDrawer`.
       - Keyboard shortcuts: Ctrl+S (Save), Del (Remove selected node).
  </action>
  <verify>test -f frontend/app/dag-studio/page.tsx</verify>
  <done>DAG Studio page assembled with comprehensive visual workflow.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Backend DAG CRUD Endpoints</name>
  <files>backend/app/main.py</files>
  <action>
    1. Add `/api/dags` GET endpoint (list saved DAGs).
    2. Add `/api/dags` POST endpoint (save/update DAG).
    3. Add `/api/dags/{dag_id}` GET endpoint (fetch single DAG).
    4. Add `/api/dags/{dag_id}` DELETE endpoint (delete DAG).
    5. Pre-seed `DAG_STORE_DB` with standard presets so fresh instances start with ready-to-run pipelines.
  </action>
  <verify>python3 -c "import sys; sys.path.insert(0, 'backend'); from app.main import app; print('DAG Endpoints Loaded')"</verify>
  <done>Backend REST endpoints for DAG CRUD implemented.</done>
</task>

## Must-Haves
- [x] Full visual DAG canvas with drag-and-drop, node selection, edge connections, and minimap.
- [x] Preset loader instantly loads sample Verdict debate architectures onto the canvas.
- [x] Backend persists DAG graphs via REST API.
