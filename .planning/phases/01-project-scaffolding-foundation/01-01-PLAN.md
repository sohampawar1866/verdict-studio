---
phase: 1
plan: 1
name: Backend Foundation (FastAPI, WebSockets, Models & Health)
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/app/main.py
  - backend/app/models/key.py
  - backend/app/models/audit.py
  - backend/app/models/dag.py
  - backend/app/models/__init__.py
requirements: [R2]
---

# Plan 01-01: Backend Foundation

## Objective
Initialize the FastAPI Python backend in `backend/` with standard directory structure, requirements, Pydantic domain models for keys, audit logs, and DAG schemas, CORS middleware, WebSocket connection hub skeleton, and health check endpoints.

## Tasks

<task type="auto">
  <name>Task 1: Create Backend Directory Structure & Requirements</name>
  <files>backend/requirements.txt</files>
  <action>
    1. Create `backend/` directory structure: `backend/app/models/`, `backend/app/engine/`, `backend/app/mcp_gateway/`.
    2. Write `backend/requirements.txt` with:
       - `fastapi>=0.115.0`
       - `uvicorn[standard]>=0.32.0`
       - `pydantic>=2.9.0`
       - `websockets>=13.1`
       - `sqlparse>=0.5.1`
       - `python-multipart>=0.0.12`
  </action>
  <verify>test -f backend/requirements.txt</verify>
  <done>Requirements file created and directory structure ready.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Pydantic Domain Models</name>
  <files>
    backend/app/models/__init__.py
    backend/app/models/key.py
    backend/app/models/audit.py
    backend/app/models/dag.py
  </files>
  <action>
    1. Create `backend/app/models/key.py` with `MCPKeyCreateRequest`, `MCPKeyRecord`, and `ToolExecutionRequest`.
    2. Create `backend/app/models/audit.py` with `AuditLogEntry`, `AuditLogStatus` (Enum: `ALLOWED`, `BLOCKED`, `VERDICT_REVIEW`).
    3. Create `backend/app/models/dag.py` with `DAGNode`, `DAGEdge`, `DAGGraph`, `DAGExecutionRequest`, `DAGExecutionResponse`.
    4. Export all models in `backend/app/models/__init__.py`.
  </action>
  <verify>python3 -c "import sys; sys.path.insert(0, 'backend'); from app.models import MCPKeyCreateRequest, AuditLogEntry, DAGGraph; print('Models OK')"</verify>
  <done>Pydantic domain models defined and importable without error.</done>
</task>

<task type="auto">
  <name>Task 3: Implement FastAPI Main Server & WebSocket Hub</name>
  <files>backend/app/main.py</files>
  <action>
    1. Create `backend/app/main.py` configuring `FastAPI(title="Verdict Studio & Haize Sentinel API", version="1.0.0")`.
    2. Add `CORSMiddleware` allowing `["http://localhost:3000", "http://127.0.0.1:3000", "*"]`.
    3. Create `ConnectionManager` class for managing live WebSocket subscribers (for live debate token streaming and real-time audit feeds).
    4. Expose `/api/health` GET endpoint returning `{"status": "ok", "service": "verdict-studio-backend", "version": "1.0.0"}`.
    5. Expose `/ws/telemetry` WebSocket endpoint for bidirectional real-time audit and DAG execution streaming.
  </action>
  <verify>python3 -c "import sys; sys.path.insert(0, 'backend'); from app.main import app; print('FastAPI app loaded:', app.title)"</verify>
  <done>FastAPI main application with CORS and WebSocket manager is fully instantiated.</done>
</task>

## Must-Haves
- [x] Pydantic models cover Key Management, Audit Logs, and DAG serialization.
- [x] FastAPI app initializes with CORS and `/api/health` endpoint.
- [x] WebSocket manager skeleton ready to stream telemetry.
