---
phase: 1
plan: 2
name: MCP Gateway Foundation (TypeScript, Stdio Transport & Dispatcher)
wave: 1
depends_on: []
files_modified:
  - mcp-gateway/package.json
  - mcp-gateway/tsconfig.json
  - mcp-gateway/src/index.ts
  - mcp-gateway/src/types.ts
requirements: [R2, R16]
---

# Plan 01-02: MCP Gateway Foundation

## Objective
Initialize the TypeScript Model Context Protocol (MCP) gateway package in `mcp-gateway/` with `@modelcontextprotocol/sdk`, strict TypeScript configuration, stdio JSON-RPC transport listener, and tool dispatch handler skeleton.

## Tasks

<task type="auto">
  <name>Task 1: Initialize MCP Gateway Package and TypeScript Config</name>
  <files>
    mcp-gateway/package.json
    mcp-gateway/tsconfig.json
  </files>
  <action>
    1. Create `mcp-gateway/package.json` with dependencies:
       - `@modelcontextprotocol/sdk`: `^1.30.0`
       - `zod`: `^3.23.8`
       - `tsx`: `^4.19.0`
       - `typescript`: `^5.6.0`
       - `@types/node`: `^20.0.0`
    2. Define CLI bin entry: `"bin": { "haize-sentinel": "./dist/index.js" }`
    3. Define build and start scripts: `"build": "tsc", "dev": "tsx src/index.ts", "start": "node dist/index.js"`.
    4. Create `mcp-gateway/tsconfig.json` with `"module": "NodeNext"`, `"target": "ES2022"`, `"strict": true`.
  </action>
  <verify>test -f mcp-gateway/package.json && test -f mcp-gateway/tsconfig.json</verify>
  <done>package.json and tsconfig.json created with valid configurations.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Stdio Server & Tool Dispatcher Skeleton</name>
  <files>
    mcp-gateway/src/types.ts
    mcp-gateway/src/index.ts
  </files>
  <action>
    1. Create `mcp-gateway/src/types.ts` defining Gateway configuration, CLI flags (`--key`, `--backend-url`, `--port`), and tool permission contracts.
    2. Create `mcp-gateway/src/index.ts` using `Server` from `@modelcontextprotocol/sdk/server/index.js` and `StdioServerTransport` from `@modelcontextprotocol/sdk/server/stdio.js`.
    3. Setup request handlers for `ListToolsRequestSchema` and `CallToolRequestSchema`.
    4. Enforce that all debugging logs are piped exclusively to `console.error` (stderr) so `stdout` remains uncontaminated for JSON-RPC frames.
  </action>
  <verify>test -f mcp-gateway/src/index.ts && test -f mcp-gateway/src/types.ts</verify>
  <done>MCP Server on stdio transport is implemented and ready for tool execution routing.</done>
</task>

## Must-Haves
- [x] TypeScript package configuration setup with `@modelcontextprotocol/sdk`.
- [x] Gateway uses `StdioServerTransport` with stderr-only logging guarantee.
