---
phase: 3
plan: 2
name: 1-Click Native Python Code Exporter
wave: 1
depends_on: []
files_modified:
  - frontend/lib/codeExporter.ts
  - frontend/components/CodeExportModal.tsx
requirements: [R8]
---

# Plan 03-02: 1-Click Native Python Code Exporter

## Objective
Build `frontend/lib/codeExporter.ts` and `frontend/components/CodeExportModal.tsx` to generate 100% syntactically valid standalone Python scripts using `haizelabs/verdict` v0.2.x from any visual DAG graph on the canvas.

## Tasks

<task type="auto">
  <name>Task 1: Implement Native Python Code Generator</name>
  <files>frontend/lib/codeExporter.ts</files>
  <action>
    1. Create `frontend/lib/codeExporter.ts` with `generateVerdictPythonCode(dag: DAGGraph): string`.
    2. Traverse nodes and topological layers:
       - Generate standard imports: `Pipeline`, `Layer`, `Unit`, `CategoricalJudgeUnit`, `CoTUnit`, `DiscreteScale`, `MaxPoolUnit`, `Schema`.
       - Generate custom `Unit` subclasses with `ResponseSchema` for custom debaters (Prosecutor, Defense, FactChecker).
       - Generate unit instance configurations with `.prompt()`, `.via()`, `.extract()`.
       - Generate pipeline composition with `Pipeline("...") >> Layer([...], repeat=N) >> ...`.
       - Generate `pipeline.run(input_data=Schema.of(...))` execution code with sample input values.
       - Include comments explaining `MaxPoolUnit` majority voting behavior (`statistics.mode`).
  </action>
  <verify>test -f frontend/lib/codeExporter.ts</verify>
  <done>Python code exporter created and tested on presets.</done>
</task>

<task type="auto">
  <name>Task 2: Build Code Export Modal Component</name>
  <files>frontend/components/CodeExportModal.tsx</files>
  <action>
    1. Create `frontend/components/CodeExportModal.tsx` displaying:
       - Modal header with Python icon and copy-to-clipboard button.
       - Syntax-highlighted dark monospaced code viewer.
       - "Download .py File" button.
       - Quick instructions on installing `pip install verdict`.
  </action>
  <verify>test -f frontend/components/CodeExportModal.tsx</verify>
  <done>CodeExportModal component created and ready for studio toolbar integration.</done>
</task>

## Must-Haves
- [x] Generated code is 100% compliant with Verdict v0.2.x specification.
- [x] Modal supports 1-click copy-to-clipboard and file download.
